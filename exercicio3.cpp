#include <iostream>
using namespace std;

int main(int argc, char** argv)
{
	int a, b, c;
	
	cout << "digite tres valores: ";
	
	cin >> a >> b >> c;
	
	if (a >= b && a >= c);
	
	cout << "maior: " << a;

    if (b >= a && b >= c);
    
    cout << "maior: " << b;

    if (c >= a && c >= b);
    
    cout << "maior: " << c;

	return 0;
}